# Test environment. Every test value lives in phpunit.xml; this file exists only so
# Laravel's environment loader finds a file when APP_ENV=testing (Pest reports the
# missing-file read as a warning on every test otherwise). Keep it empty of values.
