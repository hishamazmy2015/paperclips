<section class="section rule text-center" id="cta">
    <h2 class="h2">{{ __('site.sections.cta') }}</h2>
    <p class="lede mt-3">{{ __('site.sections.cta_text') }}</p>
    <a href="{{ $site->whatsappUrl() }}" rel="noopener" target="_blank" class="btn-primary mt-8">{{ __('site.cta.enquire') }}</a>
</section>
